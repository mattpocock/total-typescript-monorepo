export const TRANSITION_DURATION = 60;
export const DEFAULT_STEP_DURATION = 350;
export const DATA_NO_OVERRIDE_ATTRIBUTE =
  "data-no-override";
export const RESIZE_TRANSITION_LENGTH = 30;

export const featureFlags = {
  /**
   * Centres text vertically
   *
   * Not quite ready yet
   */
  USE_CENTRED_TEXT: false,
  /**
   * Uses the longest width of the code snippets
   * to calculate the font size - ensuring less
   * jumping around of code size.
   */
  USE_STATIC_WIDTH: true,
};
