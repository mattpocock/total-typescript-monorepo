// http://localhost:3004/courses/exercises/6ad7c49b-538f-4375-a73d-b884908d3724/edit

import fs from "node:fs";

const readAFileSynchronously = (path: string): string => {
  // TODO: Read the file synchronously and return its content
};

const readAFileAsynchronously = (path: string): Promise<string> => {
  // TODO: Read the file asynchronously and return a promise
  // that resolves with its content
};
