# Remotion Subtitle Renderer

Renders video subtitles using Remotion. Used as part of the video production pipeline to generate subtitle overlays.

## Commands

- `pnpm run dev` - Start Remotion Studio for preview
- `pnpm run upgrade` - Upgrade Remotion version
