# Twoslash to Simple Markdown

CLI tool for converting Twoslash-enhanced markdown to plain markdown. Strips Twoslash annotations while preserving code blocks.

## Usage

```bash
twoslash-to-simple-markdown "path/to/files/**/*.md"
```

## Commands

- `pnpm run build` - Build the CLI
- `pnpm run setup` - Link globally for local development
