# Written Content Manager

Web interface for managing TypeScript educational content. Built with Remix and backed by PostgreSQL.

## Commands

- `pnpm run dev` - Start development server on port 3004
- `pnpm run local-db` - Start local PostgreSQL via Docker
- `pnpm run migrate:dev` - Run database migrations (local)
- `pnpm run migrate:prod` - Deploy migrations to production
